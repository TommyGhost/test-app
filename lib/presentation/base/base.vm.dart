
  // bool isLoading = false;

  // void startLoader() {
  //   if (!isLoading) {
  //     isLoading = true;
  //     viewState = ViewState.Loading;
  //     update();
  //   }
  // }

  // void stopLoader() {
  //   if (isLoading) {
  //     isLoading = false;
  //     viewState = ViewState.Loading;
  //     notifyListeners();
  //   }
  // }

  // if (model.isLoading) const Center(child: CircularProgressIndicator(color: Pallet.primary,))            